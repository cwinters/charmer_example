<%= form_for(@enrollment) do |f| %>
  <% if @enrollment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
      <% @enrollment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.collection_select( :user_id, User.all, :id, :name, :include_blank => 'Choose person' ) %>
  </div>
  <div class="field">
    <%= f.label :classroom_id %><br />
      <%= f.collection_select( :classroom_id, Classroom.all, :id, :full_name, :include_blank => 'Choose classroom' ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
